import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'demo-sumup-app',
  webDir: 'dist/demo-sumup-app/browser',
};

export default config;
